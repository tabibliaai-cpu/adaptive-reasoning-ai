# Adaptive Reasoning AI — Environment Variables for Netlify
# This file is committed to the repo so Netlify picks it up automatically.

# Z.ai sandbox backend URL — the Netlify frontend proxies all AI calls here.
# Get this URL from your Z.ai preview link.
NEXT_PUBLIC_API_URL=https://preview-space.chatglm.site
